dependencies {
    compile project(":projects:version")
    compile project(":projects:list")

    compile "commons-collections:commons-collections:${project.collectionsVersion}"
    compile "commons-cli:commons-cli:${project.cliVersion}"
}